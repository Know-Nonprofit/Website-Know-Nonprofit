---
interface Props {
  as: "h1" | "h2" | "h3" | "p";
  text: string;
  class?: string;
}

const { as: Tag, text, class: className = "" } = Astro.props;
---

<Tag class={`font-inter ${className}`}>
  {text}
</Tag>
